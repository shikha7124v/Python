# create a program to ask user it's first, middle and last name

first = input("Enter your first name : ")
middle = input("Enter your middle name : ")
last = input("Enter your last name : ")

print("the initials are " + first +" "+ middle +" "+ last)
